# Ejercicios Lakeflow SDP (ubicación actual)

Los pasos guiados del track **Lakeflow SDP** viven en la raíz del repo, junto al resto de workshops:

- **`exercises/lakeflow-sdp/sdp-exercise-01-pipeline-calidad-datos.sql`** — pipeline pedidos + expectativas de calidad  
- **`exercises/lakeflow-sdp/sdp-exercise-02-cdc-produccion.py`** — CDC clientes + producción  

La preparación de datos y de Unity Catalog (esquemas, volumen `ingest.raw`, JSON y CSV) la hace solo **`generate_workshop_data.py`** (ver README principal).

El SQL del pipeline sigue en **`sdp-workshop/transformations/`**.
